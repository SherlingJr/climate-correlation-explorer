name: 'climate_explorer'
version: '1.0.0'
config-version: 2

profile: 'climate_explorer'

model-paths: ["models"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  climate_explorer:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
